# Codex base instructions

These prompt assets come from the OpenAI Codex repository at commit
`646f7c0a91b8e327d263335da68ae8ef212895ce` and are distributed under the
Apache License 2.0 in this directory.

- `gpt-5-codex.md` and `gpt-5.1.md` come from `codex-rs/core`.
- The remaining files are the bundled `model_messages.instructions_template`
  values from `codex-rs/models-manager/models.json`.

Keep the model selection in `codex_instructions.go` synchronized when these
assets are refreshed.
