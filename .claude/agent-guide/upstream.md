# 上游提供商契约

只读取本次涉及的提供商章节。通用协议转换见 [protocol.md](protocol.md)，故障切换及 OAuth 凭证终态禁用见 [proxy.md](proxy.md)，配额累计见 [billing.md](billing.md)。省略目录的 app 文件位于 `internal/app/`；文中的渠道禁用、连接轮换等跨专题机制按这些链接查阅。

审查或修改 wire 形态、指纹、签名及发送前处理时，先读该节记录的参照上游固定提交（如"对照来源"）中的对应实现，列出其完整分支表后再与本地比对，不凭推理判定"方向错误"。本地的典型偏差是只移植了命中当前场景的分支。

## CodeBuddy

- `codebuddy_oauth` 渠道支持扫码登录、workbuddy.json / canonical JSON 导入与自动刷新。登录 state 和 CookieJar 按管理员会话隔离，轮询最长 5 分钟；相同 UID + enterprise_id 重新授权更新已有渠道。
- 默认精确端点 `https://copilot.tencent.com/v2/chat/completions`，OpenAI 上游、本地协议转换。上游始终 `stream:true`；非流式调用通过共享 SSE 读取器聚合，保留工具参数、思考内容、结束原因及尾部 usage。读取到 `[DONE]` 停止；缺少 `[DONE]` 时仅接受已有完成标记的 EOF。
- 两句 Claude Code 固定文本仅在消息文本内精确替换：`official CLI for Claude.` → `official CLI tool for Claude.`（完整身份句匹配），`Main branch (you will usually use this for PRs)` → `Default branch (you will usually use this for PRs)`。工具结果和函数参数不改写。hy3 系列仅在调用方未指定思考选项时默认 `reasoning_effort:high`，渠道请求体规则可覆盖。
- CodeBuddy 复用 `antigravity_sensitive_words` 设置及匹配器，在固定文本替换后仅对 `system` / `developer` 消息的字符串或内容数组文本插入零宽字符；不改写用户/助手消息、工具参数及工具结果。空数组禁用，修改后重启生效，正常代理与管理测试共用最终化入口。
- 已知到期时间提前 60 秒刷新，HTTP 401 时强制刷新后重试一次；并发刷新按渠道和被拒 Token 合并，全凭证 CAS 保存，重新授权结果优先。刷新任务归服务器生命周期，单个请求取消不取消共享刷新。
- Bearer 和 X-Refresh-Token / 账号身份头由渠道凭证生成，刷新令牌禁止自定义规则覆盖并在调试输出脱敏；计费签到/余额请求使用独立头部，绝不携带 `X-Refresh-Token`。“获取模型”通过官方 CLI 的 `GET /v3/config` 读取 `data.models[].id`，使用账号凭证和渠道代理；已保存渠道在 401 时刷新后重试一次，失败或空目录直接报错，不回退内置列表。浏览器 OAuth、认证文件及批量凭证导入新建渠道共用该模型查询、过滤和排序逻辑，查询失败不创建渠道；已有渠道重新授权保留自定义模型配置。余额通过 `www.codebuddy.cn` 的 `daily-checkin` / `get-user-resource` 接口查询，结果进入 OAuth 用量快照并纳入单个、批量和活跃渠道额度刷新；管理端可通过 `POST /admin/channels/:id/codebuddy-checkin` 立即签到并同步余额。
- 来源及许可见 `internal/codebuddyauth/UPSTREAM.md`。未修改 CLIProxyAPI 核心快照。

## OpenCode Go

- `opencode_responses.go` 的 namespace 兼容仅用于实际目标 URL 属于 `https://opencode.ai/zen/go`、实际上游模型为 `muse-spark-1.3-contributor` 的 Responses 请求。请求体规则之后展开顶层及动态工具声明的 namespace，同步映射工具历史和 tool_choice，保留 schema、参数和 call_id；直接工具名优先保留，名称冲突或超过 64 字节时生成稳定别名。JSON/SSE 返回恢复原工具身份，同请求局部重试保留映射。其他模型、URL 和协议不触发。
- 上游最终工具参数无效、缺少声明的必填参数或终结事件参数矛盾时记录不含参数内容的 WARN；保留上游参数和状态，不将空参数伪造成 `{}`。Debug 捕获保留上游原始响应与下游恢复后的响应。

## Codex

- **Codex 模型目录**(`proxy_gemini.go`+`proxy_codex_models.go`):仅 `GET /v1/models?client_version=<非空>` 在现有 Token 渠道/模型过滤后返回 Codex `models/slug` manifest;无/空版本保留原 OpenAI/Anthropic 响应。目录只从本地已启用渠道模型名构建,不逐请求查询上游;仅列出当前 Token 可访问、至少有一条按渠道协议模式与 URL 启用状态可尝试 Codex Responses 的路由,并排除已知仅供 Images API 使用的模型、嵌入模型及目录输出模态明确不含文本的模型;未知自定义别名沿用管理员配置,不按 `gpt` 前缀过滤。此目录是配置与已知元数据的静态判断,不对每个模型发起探测请求。已知模型取静态目录的上下文与图片能力,OpenAI 静态模型再取推理档位,`gpt-6-sol/luna` 与 `gpt-4o` 使用本地预设,未知模型仅声明文本/无推理和保守的 128K 上下文。`max_context_window=null` 不阻断客户端自定义上限;ETag 按最终响应体计算,支持条件请求 304,响应标记 private/no-cache
- **Codex 图片模型**(`codex_images.go`、`admin_testing_image.go`):`gpt-image-2.5`、`gpt-image-2.5-flare`、`gpt-image-2.5-sunburst` 的公开 `/v1/images/generations`、`/v1/images/edits` 在 auto/local 模式按渠道 Codex 能力接入原生 OpenAI Images wire，使用 Codex OAuth 认证，URL 从 Responses 端点切到同 base 的 `/images/generations`、`/images/edits`。顶层 model 保留图片模型并去除路由/思考后缀；管理页共用模型归一、URL 和 JSON 请求构造。编辑 JSON 保留图片引用和 mask，multipart 的 image/image[]、mask 文件转 data URL；不下载远程图片，不创建临时文件。保留 JSON/SSE 响应与正整数 n，不在网关做多次生图展开。两款 `-2026-09-08` 快照继续仅在 generations 使用原有 Luna Responses 工具桥接；原生 Responses 文本主模型不替换。`gpt-image-1.5`、`gpt-image-2` 的管理页直连维持原状。参考 CLIProxyAPI `d1a024e9400bc65bd78ccd908945cf2eacc2835e`（checkout `e8399ffe8aeb48917df1730db93e5b5071c05cd1`）；这是 app 层移植，不更新转换核心快照。
- **Codex 窗口头**(`codex_credentials.go`+`codex_upstream_websocket.go`):`X-Codex-Window-Id` 按 CLIProxyAPI 默认行为仅随 HTTP 上游请求透传;准备原生 WebSocket 时移除从 HTTP 候选继承的窗口头,再单独应用该头的渠道自定义规则。WebSocket 握手失败回退 HTTP 时保留原窗口头;窗口变化不改变 WebSocket 默认连接复用或执行会话身份
- **Codex 上游 Header 契约**(`codex_credentials.go`+`codex_upstream_websocket.go`):不走通用反代透传;HTTP 只接收 Codex 客户端白名单,静态 Key 与 OAuth 都只用 `Authorization: Bearer`;当 `User-Agent` 能识别为官方 Codex 客户端时原样保留 UA,不要求携带 `Version`;客户端提供的 `Version` 原样保留,未提供则不补。缺少官方 UA 或任意第三方值才回退内建 UA/Version,`Originator` 固定重建。HTTP 与原生 WebSocket 都保留规范 `Session-Id`+`Thread-Id`;OAuth 和跨协议缓存路径需要补齐会话头时使用 `Session-Id`,不主动生成旧 `Session_id`/`Conversation_id` 别名。WebSocket 额外接收 turn state/timing/`OpenAI-Beta`,握手前删除 HTTP 传输头并归一 `OpenAI-Beta`,缺少规范会话头时仍可从客户端旧 `Session_id` 补齐 `Session-Id`;渠道自定义 Header 规则仍可显式增加非认证 Header
- **Codex 凭证两种形态**(`codexauth/credential.go`+`admin_codex_auth.go:HandleCreateCodexPersonalAccessToken`,`POST /admin/codex/personal-access-token`):OAuth 与个人访问令牌(PAT)共用 `auth_type=codex_oauth`,靠凭证内 `AuthMode=personalAccessToken` 区分,别新造 auth_type。PAT 必须 `at-` 前缀,导入时走 `whoami` 端点校验并解析账号身份,按账号复用或新建渠道(上游 401/403 返回 400,其余网络/服务端错误返回 502);PAT 是静态令牌,`Refresh` 直接返回 `ErrPersonalAccessTokenCannotRefresh`,一旦被上游拒绝即终态,触发上面的渠道禁用
- **Responses 路径别名**(`protocol/types.go`+`server.go`+`codex_wire.go:normalizeCodexClientPath`):下游 `/v1/responses`、`/v1/codex/responses`、`/backend-api/codex/responses` 是同一 canonical 端点,GET 走 WebSocket 升级、POST 走 SSE fallback,`DetectRequestFamily` 一律识别为 Responses。上游协议为 Codex 时转发前把别名归一回 `/v1/responses`,避免别名泄漏进上游 URL;`Exact` 标记的 OAuth 官方 URL 不拼接路径,不受影响。新增 Responses 入口路径必须同时登记这三处
- **Codex 与 Anthropic 上游 TLS** (`codex_utls_transport.go`): 普通 HTTP 请求对 `https://chatgpt.com` 使用 Chrome uTLS，按 HTTP/2 → uTLS HTTP/1.1 → 标准 HTTP/1.1 降级；`https://api.anthropic.com` 使用 Node 风格 HTTP/1.1 uTLS 与每个 Transport 的 32 项 TLS session cache。Anthropic ticket 随账号、代理与连接代际隔离；其他 Host 走标准 Transport。uTLS 继承环境/渠道代理、Host 覆盖、证书校验和握手超时，连接池跟随 `upstream_connection_reuse_limit_seconds` 整代轮换；原生 WebSocket 使用独立 Dialer。
- **OAuth 连接池隔离** (`server.go:getClientForChannel`): 所有 OAuth 渠道经此入口发出的 HTTP 数据、凭证刷新、额度及模型目录请求，使用按账号和渠道代理 URL 隔离的 Client/Transport；优先取稳定账号身份，避免 access token 轮换重建连接池，同渠道更换账号必须切换池。Antigravity 按 refresh token 隔离，Anthropic 及其他 OAuth 还包含渠道 ID；API Key 渠道沿用默认或按代理共享的池。Codex 原生 WebSocket 的物理连接独立属于执行会话，复用时核对渠道和认证信息。
- **Codex 多代理 v2**(`codex_multi_agent_v2.go`):只有一条规则——**Codex 协议 → 非 Codex 协议**的 `agent_message` 与 collaboration 工具可移植性改写,按官方 Codex 客户端 User-Agent(`isCodexMultiAgentClient`)门控,其他调用方一律原样透传,避免把网关模型目录写进第三方请求体。Codex → Codex 透明直通。不存在命名空间改写-还原对:非 Codex 源协议转成 Codex 后是扁平 `tools` 数组,不携带 `type=namespace` 包装,无从改写

## Cursor

- **Cursor SDK Bridge 契约**(`cursorauth/`+`app/cursor_wire.go`+`app/cursor_credentials.go`+`app/admin_cursor_auth.go`):`auth_type=cursor_oauth`。只接受 `POST /admin/cursor/credentials/import` 导入 User API Key，并通过 `POST /auth/exchange_user_api_key` 换取控制面会话；浏览器 PKCE 和直接粘贴 `accessToken` 不支持，因为两者不能提供推理必需的 API Key。身份/额度走 Connect JSON 一元 RPC：`DashboardService/GetMe`、`DashboardService/GetCurrentPeriodUsage`；模型目录只走 SDK Bridge `SdkCursorService/ListModels`，为声明 `fast=true` 变体的 `SdkModel.id` 派生 `-fast` 模型名，请求时还原成基础 ID 与 `ModelSelection.params`。渠道 URL 固定 `https://api2.cursor.sh`，协议 `anthropic,openai` + local 转换。推理不 HTTP 转发：启动时在后台查找或安装锁定版本 `cursor-sdk-bridge`，校验 SHA-256 后启动探活；自动发现的同目录、PATH 和缓存二进制必须匹配 `bridge.lock` 中的 `binary_sha256_*`，旧文件不匹配时改用锁定版本安装目录，不覆盖旧文件。只有显式 `CURSOR_SDK_BRIDGE_BIN` 跳过发行版本校验，但仍须通过探活、协议与能力检查；上游 `GetVersion` 返回服务版本而非 SDK 发行版本，不能用于自动升级判断。升级快照时须从已校验的五平台发布包重新计算可执行文件摘要；Bridge 异常退出由进程级 watcher 单飞重启，只有无副作用的 `ListModels` 可在确认旧 client 脱离后重试；Cursor 明确拒绝凭证时重铸会话，下游尚无语义输出才最多重试一次，其他 `CreateAgent`/`Send` 故障和已开始输出的流禁止重放。经 loopback Connect 执行 `CreateAgent → Send → DeleteAgent`，每个 RPC 显式传 User API Key。Agent 的 built-in allowlist 只开放 `mcp` capability group，因为 SDK custom tools 通过 Cursor 合成的 `custom-user-tools` MCP server 暴露；shell、文件等其他内建工具仍禁用。客户端函数必须通过 `LocalAgentOptions.custom_tools` 原生注册；ccLoad 在鉴权 loopback 上实现 `SdkCustomToolCallbackService`。回调到达后按 `agent_id` 定位挂起 session、按 `call_id` 返回 Anthropic `tool_use` 或 OpenAI `tool_calls`，客户端下一轮交回结果后解锁同一 Cursor run；同一渠道允许多个 session 并发，禁止全局“当前会话”、prompt 工具协议、工具名猜测或参数别名改写。ccLoad 只中转客户端工具结果，不在网关执行客户端工具。额度快照写入凭证 `oauth_usage`，不记 `quota_cost_usage`

- **Cursor 用量收尾**(`cursorauth/sdk_runner.go`):终态缺少 usage 时，`GetRun` 补查在取消 session 前执行并继承 Agent 的 `runCtx`，附着请求取消、Agent 超时或 runner 关闭都应终止补查。`DeleteAgent` 仍使用独立清理上下文，避免请求取消导致 Agent 残留；工具会话继续支持跨 HTTP 请求恢复。

## Z.ai

- **Z.ai Coding Plan 契约**(`zaiauth/`+`app/zai_wire.go`+`app/zai_credentials.go`+`app/admin_zai_{auth,oauth}.go`):`auth_type=zai_oauth`,凭证里真正用于转发的是长期有效的 Coding Plan API Key(`id.secret`),`access_token` 只用于在 Key 被 401 拒绝后重新派生(`/api/auth/z/login` → `getCustomerInfo` → 复用或新建名为 `zcode-api-key` 的 Key → copy secretKey),没有过期时间也没有定时刷新。渠道 URL 是 ZCode 路由后的端点 `https://zcode.z.ai/api/v1/ultra-zai/anthropic`(创建渠道时按 `/api/v1/agent/configs` 的 `proxyEndpoint.mapping` 解析,失败回退内置常量),协议固定 anthropic + local 转换;该 URL 含 `/v1`,`validateChannelBaseURL` 对 `zai_oauth` 专门放行。转发时重建请求头:只用 `x-api-key`(删除 Authorization)、`User-Agent: ZCode/<ver>`+`X-ZCode-App-Version`+`X-Title`+`X-ZCode-Agent`+`HTTP-Referer`+平台头,并把 body 的 `metadata.user_id` 覆盖为 ZCode 指纹 `{"device_id","account_uuid":"","session_id"}`(字段顺序是契约的一部分;device_id 由账号身份派生,session_id 优先取请求自带的)。模型目录动态拉取,三级来源:①账号目录 `https://api.z.ai/api/coding/paas/v4/models`(Coding Plan 自己的目录,新模型先到这里)+通用 API `/api/paas/v4/models` 补齐;②models.dev 的 `zai-coding-plan` provider(无需 Key,仓库已为定价同步该站);③`zaiauth.DefaultModels` 兜底。导入渠道时即按实时目录建模型列表,不靠发版更新。额度查询走 Z.ai 订阅面板内部端点 `https://api.z.ai/api/monitor/usage/quota/limit`(未公开文档,接受 Coding Plan Key):`data.limits[]` 每项给 `type`(`TOKENS_LIMIT` 等)+`unit/number`(3/5=5 小时,6/1=周)+`percentage`(已用 0-100)+`nextResetTime`(毫秒),该端点即使 Key 无效也返回 HTTP 200,成败以信封 `success`/`code` 为准;归一成 `oauthUsageWindow` 后按普通 OAuth 渠道流程持久化进凭证 `oauth_usage`,但**不记 `quota_cost_usage`**(Coding Plan 按额度计费,标准成本窗口对它没有意义)。登录是轮询式:`POST /admin/zai/oauth/start` 起 flow 后由服务端后台轮询 `/oauth/cli/poll/{id}`,不需要粘贴回调;上游该端点当前返回空 404 时统一报 `ErrOAuthFlowUnavailable`(503),此时走 `POST /admin/zai/credentials/import` 直接导入 Coding Plan Key

## Zed

- **Zed 原生登录契约**(`zedauth/`+`app/zed_wire.go`+`app/zed_credentials.go`+`app/admin_zed_auth.go`):`auth_type=zed_oauth`。这不是 OAuth code/PKCE：每次登录在随机 loopback 端口生成临时 RSA-2048 密钥，把 PKCS#1 DER 公钥以 base64url 传给 `zed.dev/native_app_signin`，回调的 `access_token` 用 RSA-OAEP/SHA-256 解密成长期 native credential；临时私钥绝不持久化。`system_id` 是可选的 Zed 安装标识：按登录表单显式值 → `CCLOAD_ZED_SYSTEM_ID` → 本机 Zed `db/0-global/db.sqlite` 的 `kv_store.system_id` 取值；没有可信来源时允许发起登录，但请求省略 `x-zed-system-id`，由上游决定账号是否具备试用权限；禁止生成随机值或复制其他机器的固定值；同账号重授权必须保留已存值。数据请求先用 `Authorization: <user_id> <native-json>`（有值时附加 `x-zed-system-id`）调 `/client/llm_tokens` 换短期 JWT，再以 Bearer 调 `/completions`；JWT 提前 60 秒单飞刷新并 CAS 持久化，401 刷新必须绑定被拒绝的 JWT，迟到的旧 401 不得再次 mint。渠道固定 exact `/completions`、codex 协议+local 转换、禁用 WebSocket；动态暴露 `/models` 中 ccLoad 已实现 wire 转换的 `open_ai`、`anthropic`、`google` 模型。请求将标准 Responses body 按模型 provider 转换后包进 Zed `thread_id/prompt_id/intent/provider/model/provider_request` envelope，展开 Zed 不支持的 namespace 外壳并限定子工具名、合并 `additional_tools`、归一工具调用历史和 developer role/null 字段及 GPT-5.6 reasoning；OpenAI 响应恢复 namespace 工具身份，其他 provider 的逐行 `{"event":...}` 转换为标准 Responses SSE，收到结构化 `{"status":"stream_ended"}` 立即关闭上游；`plan` 403 只冷却当前模型并切换渠道，其他 401/403 才刷新凭证。额度用 native credential 读取 `/client/users/me`，快照写入 `oauth_usage`，不记 `quota_cost_usage`。渠道 `custom_request_rules` 的 body 路径必须按 **Codex 格式**书写（如 `reasoning.effort`），不能写 `provider_request.*`——body rules 在 `prepareTranslatedUpstreamBody` 阶段作用于 Codex body，早于 Zed finalizer 把它包进 `provider_request` envelope

## Antigravity

- **输出上限**：Claude 目标在 `prepareAntigravityRequestBody` 按实际上游模型名（`RequestModel`，目录 ID 大小写归一）对照 Antigravity 静态目录 `MaxCompletionTokens` 裁剪超限的整数字段 `generationConfig.maxOutputTokens`；未知模型或未登记上限时保留原值，不补写缺失字段。非 Claude 目标继续删除该字段。该规则对齐 CLIProxyAPI executor 的发送前处理，不能只同步转换器而遗漏此边界，也不能从 URL 路径刮模型名。

- **Antigravity 上游契约**(`server.go`+`antigravityauth/service.go`+`antigravity_wire.go`+`upstream_connection_age.go`):启动时以 `electron-builder` UA 从官方 Hub manifest 读取三段式版本,失败回退 `antigravity/hub/2.9.1 darwin/arm64`;非流请求走 `/v1internal:generateContent`,流式请求走 `/v1internal:streamGenerateContent?alt=sse`,但两者共用 daily → daily sandbox 的地址回退策略;Cloud Code 数据请求及项目/模型/额度查询共用该 UA 和按 refresh token 隔离的标准 HTTP/1.1 连接池,OAuth Token 端点仍按原生契约使用 `Go-http-client/2.0`;不用 uTLS、不先试 HTTP/2,避免在 Google Cloud Code 内部端点上重放失败的 POST;连接复用、每主机空闲数和空闲超时继承统一上游 Transport 策略,无代理与渠道代理池继续保持隔离并服从 `upstream_connection_reuse_limit_seconds`。Claude 目标请求必须在每个 model turn 的首个 `functionCall` 写入 `skip_thought_signature_validator`,同 turn 的并行 sibling 不带签名;所有已支持入口统一在 provider finalizer 收敛,禁止依赖 400 后剥离工具历史的降级重试
- **Antigravity 签名恢复**(`antigravity_replay.go`):由 app 持有有界内存缓存，纯转换器不持有跨请求状态。恢复 Anthropic thinking signature、Responses reasoning encrypted_content 及文本签名载体；按调用方凭证、会话、渠道账号、模型、上游地址和入口协议隔离，缺少调用方或稳定会话标识时不恢复，不用 prompt_cache_key 代替会话。仅完整成功响应入缓存，显式签名优先；thinking 有效期 3h，Responses 为 1h，总上限 1024 条/8MiB。签名 400 使当前作用域失效，不剥离思考或工具历史后重试。
- **Antigravity Claude 工具结果顺序**(`antigravity_wire.go:prepareAntigravityRequestBody`):四种客户端入口统一在 provider finalizer 将 user turn 的 `functionResponse` 稳定排列到普通内容之前，保留工具 ID、结果载荷、嵌套图片及其相对顺序，不改 model turn 或跨 turn 顺序。中途 system/developer reminder 合并后也必须遵守此规则；共享 Gemini/Vertex 的文本优先规则保持不变。
- **Antigravity Responses 搜索**:只有仅含原生 web_search 工具且 tool_choice 允许时进入专用搜索请求，使用 Gemini 搜索模型并透传域名过滤；混合函数工具或 tool_choice=none 不启用原生搜索。流式与非流式返回 web_search_call、引用注解和搜索使用量，按实际发出的 provider 请求判断搜索模式。OpenAI Chat 中途 system/developer 消息使用 `<system-reminder>`，保留工具调用配对；Claude usage/cache token 和流结束修正继续保留。
- **Antigravity 容量错误**:精确 `MODEL_CAPACITY_EXHAUSTED` 首次出现时立即按原始 503 冷却当前实际上游模型(默认 2 min),默认在 daily 与 daily sandbox 间重试;自定义 URL 列表仍最多尝试 3 个。重试成功立即清除该模型冷却,全部失败保留上游 503 供诊断、对外统一为 429。容量/URL 回退失败的日志延迟到重试器定论后只落一条,避免一次逻辑请求产生多条失败日志;模型发现不因该错误冷却 URL
- **Antigravity 额度耗尽**:只有实际上游模型为 Claude 时,`QUOTA_EXHAUSTED` 才写入独立 `StandardQuota` 状态,保留积分回退路径。Gemini 等不支持积分回退的模型走通用冷却,按上游 `metadata.model` 与精确 reset 时间冷却模型并切换渠道;所有配置模型均冷却时升级渠道冷却,管理测试与代理遵循相同规则。

## OAuth 全局上游地址

- **OAuth 全局上游地址**:`CODEX_BASE_URL`(完整 Responses URL)、`XAI_BASE_URL`(API 根地址,通常以 `/v1` 结尾)、`ANTIGRAVITY_URL`、`ANTHROPIC_BASE_URL`(API 根地址)默认均为空;设置页分别以 `https://chatgpt.com/backend-api/codex/responses`、`https://cli-chat-proxy.grok.com/v1`、`https://daily-cloudcode-pa.googleapis.com`、`https://api.anthropic.com` 显示默认渠道地址占位提示,Antigravity 另有 `https://cloudcode-pa.googleapis.com` 备用地址。非空时对应 OAuth 渠道的数据请求、模型发现、测试和额度查询只使用该全局地址并忽略渠道 URL;OAuth 授权、Token 交换/刷新仍使用提供商官方地址,API Key 渠道不受影响。属于系统设置,修改后重启生效

## xAI

- **xAI Responses 图片工具**(`xai_wire.go`,`xai_images.go`):`image_generation` 只保留给 `grok-4.6` 及之后的对话模型;`grok-4.20-*` 是旧产品线,不能按小数版本误判为比 4.6 新。客户端强制选择 `{"type":"image_generation"}` 必须改写成 xAI 接受的 `allowed_tools`+`mode=required`;不支持的模型删除该工具及孤立选择。公开 `POST /v1/images/generations` 使用 `grok-4.6+` 时,app 层把 OpenAI Images 请求桥接成 xAI Responses 工具调用:非流请求聚合成标准 Images JSON,流请求输出 `image_generation.partial_image/completed` SSE,已提交后断流补 `event:error`;这不是 xAI 原生 `grok-imagine-*` 媒体 API,不要把两条线塞进同一个 finalizer

## Anthropic / Claude Code

- **对照来源**: Anthropic 原生传输参照 CLIProxyAPI `21d26a07a39316f94c6ebc370a92222bca89aaac`；账号身份、模拟 beta/header 参照 sub2api `a3eb7ef302961cba716dc78b39b93b60c467db0e`。两者均为 app 层参照，不改变 `internal/protocol/cliproxy/UPSTREAM.md` 的完整同步基线。

- **Anthropic thinking**:项目生成的 Anthropic 请求用 `thinking.type=adaptive` + `output_config.effort`;anyrouter `/v1/messages` 兜底补 adaptive 并归一旧 `enabled`;anyrouter 额外注入 `anthropic-beta: context-1m`
- **Claude Code CLI 指纹** (`anthropic_wire.go`): 原生 `/v1/messages` 判定遵循 sub2api 的 body 边界：合法 Claude CLI UA 加可解析 `metadata.user_id`，或非空 `user_id` 加 billing block 以恢复被代理替换的 UA；`X-App`、beta 和 CCH 不作为必要条件。按改写前的调用方 body 判定，保留原生 system、CCH 和身份头；空 text、未获最终 beta 授权的字段、延迟加载工具与 thinking/redacted_thinking 块上的 cache_control 以及超过 4 个的缓存断点会被清理（`applyAnthropicMessagesAPIInvariants`，对齐 sub2api）。OAuth 出站（原生、模拟及 count_tokens）把 system 文本和 messages 文本中 `<system-reminder>` 内的 `Today’s date is …` 还原为 ASCII 撇号加 `-` 分隔，抹掉客户端发现非官方 base URL 时写入的中转标记；用户正文、tool_result 与 API Key 渠道不改，改写先于调用方 CCH 重签。OAuth 出站仍有可解析 user_id 时，无论原账号是否相同，均把 JSON/legacy 身份重映射到目标账号；只有邮箱的凭证（setup-token 等）同样重映射，device 取账号级 ClientID、`account_uuid` 发空（与模拟路径和真实 CLI 一致），不透传调用方身份；JSON 只含 device_id、account_uuid、session_id，显式 BodyRules 删除的 user_id 不重建。原生和模拟路径共用账号级 device_id；只有原本带会话头且 body 仍有映射会话时才同步头。模拟 `/v1/messages` 生成 billing 与身份 system，OAuth 使用固定 beta、Linux/arm64/Node 指纹，缺失 metadata 时补账号身份，不主动发送会话头；模拟头在自定义 HeaderRules 后再次固定，以保持 UA 和 billing 版本一致。API Key 仍按 body 选择 beta，但不发 `oauth-2025-04-20`（CPA：该 beta 只属于 OAuth 凭证）；两条模拟路径的 `X-Stainless-OS/Arch` 都固定 Linux/arm64，不取网关宿主机平台；Fable 使用两段 system，OAuth 模拟工具增加缓存断点。账号指纹只采纳原生请求中同主版本、且不超前运行时版本太多（同 minor 超前 ≤100 个 patch，或下一 minor 的 patch ≤100）的 Claude CLI UA 和随附 Stainless 头，缓存按账号隔离，数据库写入在状态锁外串行执行，失败后退避重试；相同版本的原生客户端可修正头元数据。已存版本低于运行时版本下限时只在读取时抬升，不写库，避免版本同步后逐账号重写凭证、失效渠道缓存；Stainless SDK 同理，低于内置配对（CPA 实测 Claude Code 2.1.280 ↔ `@anthropic-ai/sdk` 0.112.1 ↔ Node v26.3.0）的已存或学到的包版本连同运行时版本一起换成内置配对。Haiku helper 识别只校验 Stainless 包/运行时版本形态，不钉具体版本。
- **OAuth 模拟工具别名** (`anthropic_mcp_alias.go`，对齐 CPA MCP 别名): 仅 OAuth 模拟路径（非原生 Claude Code、非 Haiku helper）的 `/v1/messages` 与 count_tokens 改名，在最终化之后、出站之前执行。无 type 或 `type=custom` 的客户端工具改为 `mcp__<词>_<词>__<词>_<原名净化截断>`（BIP-39 词表，HMAC 密钥为渠道 ID + 账号身份种子，不取 access token，保证跨请求稳定、不击穿 prompt cache），并去掉 `type=custom`；已是合法 `mcp__server__tool` 的名字、Anthropic 服务端工具（`web_search_*` 等）及与其同名的客户端工具不改。同步改写 `tool_choice`（type=tool）与历史 `tool_use`/`tool_reference`/`tool_search_tool_result` 引用。映射只属于本次请求，经 `upstreamWireAliases` 随同请求 400 重试回放携带；响应在透传与协议转换前按结构还原 `tool_use.name`、`tool_reference.tool_name`（非流式整包、流式逐 SSE 事件），不做正文字符串替换，映射外名字原样透传。
- **Claude Code CLI 版本** (`anthropic_cli_version_sync.go`): OAuth 模拟 UA 与 billing `cc_version` 同取运行期有效版本，API Key 模拟请求保留原有继承策略。默认每小时同步官方稳定版，内置 2.1.280 为下限；最近一次成功版本写入 `data/anthropic-cli-version.json`（或 `CCLOAD_ANTHROPIC_CLI_VERSION_CACHE`），重启时先加载。`CCLOAD_ANTHROPIC_CLI_VERSION_SYNC=false` 关闭网络同步但继续使用缓存。
- **原生 Claude Code 透传** (`anthropic_wire.go`，对齐 CPA): 原生请求除固定白名单外，还透传 `x-claude-code-*`、`x-claude-remote-*`、`x-client-app`、`x-anthropic-additional-protection`，保证上游能识别 subagent、压缩和远程容器请求。转发到 OAuth 渠道时，如果调用方声明了非空 beta 且不含 `oauth-2025-04-20`，说明它把上游当成了 API Key，此时补回 OAuth 凭证本身的 beta：插入 `oauth-2025-04-20`，并追加 `extended-cache-ttl-2025-04-11`。以下请求不追加 extended-cache-ttl：subagent（Agent-Id/Parent-Agent-Id 头、`parent_session_id`、`cc_is_subagent=true`）且未自带 1h cache_control 的，`max_tokens=1` 探针，以及只要 `title` 的标题 helper；count_tokens 也不追加。调用方已声明 oauth beta 时原样保留。目标是第一方 base URL 时，缺失 `x-client-request-id` 会生成 UUID，并固定 `Connection: keep-alive`。
- **Claude Code beta 集合** (`anthropic_wire.go`): OAuth 模拟请求使用 sub2api `FullClaudeCodeMimicryBetas` 的固定九项集合；API Key 模拟请求仍按 body 实际字段声明附加 beta，基础集合不含 `oauth-2025-04-20`。`/v1/messages/count_tokens` 额外声明 `token-counting-2024-11-01`，并移除 `max_tokens` 等生成参数。
- **Anthropic 缓存窗口归调用方** (`anthropic_wire.go`): 5m 或 1h 仍由调用方 body 决定。网关注入的 system breakpoint 跟随已有 1h 窗口；OAuth 模拟请求虽然按 sub2api 固定声明 `extended-cache-ttl-2025-04-11`，也不会主动把 5m 升为 1h。
- **Anthropic count_tokens**: 仅当候选渠道的所有运行时 URL 都是 `https://api.anthropic.com` 时请求上游；第三方 URL、无候选、令牌模型白名单或渠道限制排除官方渠道、以及官方源站失败时使用本地估算。本地估算不按模型名拒绝；数组形式的 tool_result 和 text/content 文档按实际内容计，只有二进制 PDF 使用固定估值。不写共享模型/URL 冷却、请求日志与 Token 统计（请求日志会进入渠道健康度），上游失败只打 `[WARN]` 服务日志。400/413 视为请求体问题，不再扇出到其他官方渠道；其余错误换下一个官方渠道。官方源站的 OAuth 和 API Key 请求都删除 metadata、context_management、diagnostics 与生成参数；第三方的原生字段由最终化函数保留。count_tokens 与 Messages 共用 `applyAnthropicMessagesAPIInvariants`（空 text、非法 cache_control、断点上限），计数请求与实际请求被接受的 body 一致。真实 Claude CLI 的 count_tokens 只凭合法 UA 识别原生 wire，不要求 metadata。两条 OAuth 头路径都带 token-counting beta；官方源站不转发无 body 身份对应的会话头。
